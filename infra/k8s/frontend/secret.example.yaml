# Vorlage - kopieren nach secret.yaml (gitignored) und echte Werte eintragen,
# oder äquivalent per `kubectl create secret generic frontend-secret ...`.
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: dorfpartys
type: Opaque
stringData:
  AUTHENTIK_CLIENT_ID: ''
  AUTHENTIK_CLIENT_SECRET: ''
