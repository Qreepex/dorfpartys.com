services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dorfpartys
      POSTGRES_PASSWORD: dorfpartys
      POSTGRES_DB: dorfpartys
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
